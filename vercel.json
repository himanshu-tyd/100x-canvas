{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/next"
        }
    ],
    "buildCommand": "pnpm install && pnpm build",
    "installCommand": "pnpm install",
    "outputDirectory": ".next"
}